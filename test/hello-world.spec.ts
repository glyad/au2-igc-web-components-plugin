// import { IgcInputComponent } from 'igniteui-webcomponents';
// import { createFixture } from '@aurelia/testing';

import { assert } from "console";

describe('my-app', () => {
  it('should just work', async () => {
    assert(true, 'This is a placeholder test that should always pass');
  });
  // it('should render message', async () => {
  //   const { assertText } = await createFixture(
  //     '<hello-world message.bind="message"></hello-world>',
  //     { message: "foo" },
  //     [HelloWorld],
  //   ).started;

  //   assertText('Hello World! foo', { compact: true });
  // });

  // it('should render value', async () => {
  //   const { assertText } = await createFixture(
  //     '<igc-input value.bind="message"></igc-input>',
  //     { message: "foo" },
  //     [IgcInputComponent],
  //   ).started;

  //   assertText('foo', { compact: true });
  // });
});
